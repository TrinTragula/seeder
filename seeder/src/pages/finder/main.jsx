import { mountPage } from '../../shared/boot';
import Layout from '../../shared/Layout';
import FinderPage from './FinderPage';

mountPage(<Layout variant="page" width="wide" path="/finder/"><FinderPage /></Layout>);
